Optimize one assigned four-input signed 9-bit Q2.7 `a*b + c*d` candidate by redesigning a small compressor or carry-reduction cone near the Q4.14-to-Q4.7 quantization boundary.

The hard acceptance constraint is `evm_db <= -54 dB` under the existing authoritative `eval.py`. EVM is negative, so more negative is better. Reject or revert every candidate above -54 dB. Optimize output-reachable weighted gate cost with AND=1, OR=1, XOR=3, and NOT=0. Modify only the assigned candidate under `generations/`; never alter parents, initial solutions, evaluators, gate costs, or infrastructure.

Do not use another broad symmetric low-column cutoff as the primary mutation. Existing runs have already explored pruning columns 0-5, restoring selected column-5 Booth terms, fixed compensation around columns 5-7, top Booth-mux factoring, constant folding, and structural hash-consing. Instead, inspect the actual column-reduction network and find a small output-reachable cone in columns 5-8 whose sum or carry can affect output bit 7 or higher.

Prioritize significance-driven approximate compressor design. Candidate transformations include replacing one exact 3:2 compressor with a cheaper approximate sum/carry function, preserving carry while approximating the low-significance sum, simplifying only the sum output of an XOR-heavy full adder, or merging adjacent low-column compressors so that an inexpensive shared carry predictor replaces multiple exact XOR paths. Prefer errors confined to the lowest useful weight and avoid changing signed high columns, sign correction, or overflow behavior.

For a selected local cone with at most eight live inputs, derive its exact truth table and use the available exact truth-table synthesis tool to establish the cheapest exact implementation first. Then consider a deliberately defined approximate truth table that changes only low-impact rows. Compare variants such as exact carry with approximate sum, OR-based carry, majority-preserving carry, and input-pattern-selective sum suppression. Do not synthesize the complete multiplier.

The target is a new Pareto point better than the existing low-cost region around gate cost 1069 with EVM about -54.21 dB, or a comparable cost with materially more EVM margin. A candidate that merely reproduces an existing cost/EVM point is not useful.

Preserve the zero-argument `build_circuit()` interface, static input-independent Circuit DAG, fixed least-significant-bit-first `a`, `b`, `c`, `d` input ordering, 19 Q4.14 output bits, signed two's-complement semantics, and the allowed AND, OR, XOR, and NOT gate set. Validate import and DAG structure, measure reachable weighted cost, and run the unmodified `eval.py` on the exact final file. Keep only a measured candidate satisfying EVM <= -54 dB.
