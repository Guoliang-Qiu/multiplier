Perform Pareto-guided local logic resynthesis on one assigned four-input signed 9-bit Q2.7 `a*b + c*d` candidate. The goal is to discover a structurally different local replacement, in the spirit of evolved approximate-circuit libraries, rather than applying another predetermined truncation pattern.

The hard acceptance constraint is `evm_db <= -54 dB` under the existing authoritative `eval.py`. EVM is negative, so more negative is better. Any result above -54 dB must be rejected. Optimize output-reachable weighted cost with AND=1, OR=1, XOR=3, and NOT=0. Modify only the assigned candidate under `generations/`.

First inspect the parent and identify expensive output-reachable cones, especially cones containing multiple XOR gates, full adders, Booth selection muxes, or carry logic in columns 5-10. Exclude signed high-column and overflow logic unless equivalence can be established. Select a cone with a small external interface, preferably no more than eight live inputs and one or two outputs.

For each selected cone:
1. Derive the exact local truth table and synthesize or reason about a cheaper exact AND/OR/XOR/NOT implementation.
2. If no useful exact saving exists, construct a small family of approximate truth tables by changing only low-significance output cases.
3. Preserve the more significant carry output whenever possible and approximate the lower-weight sum output first.
4. Consider output don't-cares only when they are justified by unreachable internal states or by an explicitly accepted approximation; never invent don't-cares silently.
5. Evaluate replacements inside the complete multiplier because isolated truth-table error does not predict final Q4.7 EVM.

Use the available exact truth-table synthesis tool for small cones. Explore AND/OR-heavy alternatives when they can remove weighted-cost-3 XOR gates. Also consider multi-output synthesis so sum and carry can share intermediate terms; a jointly synthesized two-output cone may be cheaper than independently rewriting each output.

Compare every final candidate against the existing Pareto region, including approximately:
- cost 1069 at EVM -54.21 dB;
- cost 1071 at EVM -55.07 dB;
- cost 1080 at EVM -55.19 dB;
- cost 1108 at EVM -55.72 dB;
- cost 1119 at EVM -57.06 dB.

Prefer a genuinely new non-dominated point. Do not keep a candidate that only reproduces an existing metric pair or merely performs already exhausted constant folding, commutative hash-consing, top Booth-mux factoring, broad column cutoff, or fixed compensation.

Preserve the zero-argument `build_circuit()` interface, fixed LSB-first `a`, `b`, `c`, `d` ordering, signed two's-complement semantics, static input-independent DAG, 19 Q4.14 outputs, and allowed gates. Validate import and circuit structure, measure the exact output-reachable weighted cost, run the existing `eval.py` on the final file, and retain only a verified candidate satisfying EVM <= -54 dB.
