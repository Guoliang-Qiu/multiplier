Inspect the parent Circuit DAG for a region that can be replaced by an
approximation, and apply a local approximation strategy that is materially
different from the strategies already tried. The objective is the four-input
sum `a*b + c*d`, with four 9-bit two's-complement Q2.7 inputs and a 19-bit
two's-complement Q4.14 output. Input buses must remain in fixed `a`, `b`, `c`,
`d` order. Explore approximation of partial products, compressors, carries,
or shared arithmetic where appropriate; do not rewrite the entire circuit
without a clear reason. Reuse or recombine beneficial parent subgraphs when
their interfaces and bit-level semantics match. Use only AND, OR, XOR, and
NOT gates, and preserve the zero-argument `build_circuit()` interface and
shared DAG format. The external evaluator uses `(a*b + c*d) / 128` as the
full-precision reference and quantizes only the circuit's Q4.14 result to
Q4.7. The approximation must be implemented in the runnable circuit itself.
Evaluate the candidate with the existing `eval.py` procedure and use its
reported `evm_db` and weighted gate cost to verify the tradeoff. Do not
implement simulation or scoring in the candidate, and do not replace the
evaluator with a local error estimate.
